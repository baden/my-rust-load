# my-rust-load
Граюсь з Copilot. Нічого цікавого тут нема.

```sh
sudo systemctl daemon-reload
sudo systemctl start file_upload_server
sudo systemctl enable file_upload_server
```

```sh
sudo systemctl status file_upload_server
```
